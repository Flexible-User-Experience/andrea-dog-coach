presta_sitemap:
    defaults:
        priority: 1
        changefreq: 'daily'
        lastmod: 'now'
    alternate:
        enabled: true
        default_locale: '%default_locale%'
        locales: '%locale_i18n_full%'
        i18n: 'symfony'
