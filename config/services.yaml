parameters:
    default_locale: 'ca'
    delivery_dev_address: '%env(string:DEV_DELIVERY_ADDRESS)%'
    locale_i18n: [ 'es', 'en' ]
    locale_i18n_full: [ 'ca', 'es', 'en' ]

services:
    _defaults:
        autowire: true
        autoconfigure: true
    App\:
        resource: '../src/'
